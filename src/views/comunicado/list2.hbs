{{>logo}}
<div class="container py-2">
    <div class="row">
        <div class="col-md-12 ">
            <h1>COMUNICADOS EN LA PARTIDA {{ partida.titulo }} - {{ partida.descripcion }}</h1>
            <div>
                <h2 class="text-center"> Listado de comunicados</h2>
                {{#if comunicados}}
                <table class="table table-dark table-striped">
                    <tr>
                        <th  class="d-none d-xl-table-cell">id</th>
                        <th>titulo</th>
                        <th >Descripcion</th>
                        <th>Fecha</th>
                    </tr>
                    {{#each comunicados}}
                    <tr class="">
                        <td  class="d-none d-xl-table-cell">
                            <p> {{this.id}}</p> </a>
                        </td>
                        <td>
                            <p> {{this.titulo}}</p></a>
                        </td>
                        <td>
                            <p> {{this.descripcion}}</p>
                        </td>
                        <td>
                            <p> {{formatearSpHora this.fecha}}</p>
                        </td>
                                </tr>
                    {{/each}}
                </table>
                {{/if}}
            </div>
        </div>
    </div>
    <div class="text-center m-2">
        <a href="/partidas/plantilla/{{partida.id}}" class="btn btn-warning"> VOLVER A LA PARTIDA</a>
    </div>
</div>