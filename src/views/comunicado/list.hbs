{{>logo}}
<div class="container py-2">
    <div class="row">
        <div class="col-md-12 ">
            <h1>COMUNICADOS EN LA PARTIDA {{ partida.titulo }} - {{ partida.descripcion }}</h1>
            <div>
                <h2 class="text-center"> Listado de comunicados</h2>
                {{#if comunicados}}
                <table class="table table-dark table-striped">
                    <tr>
                        <th>id</th>
                        <th>titulo</th>
                        <th>Descripcion</th>
                        <th>Descripcion</th>
                    </tr>
                    {{#each comunicados}}
                    <tr class="table-active">
                        <td>
                            <p> {{this.id}}</p> </a>
                        </td>
                        <td>
                            <p> {{this.titulo}}</p></a>
                        </td>
                        <td>
                            <p> {{this.descripcion}}</p>
                        </td>
                        <td>
                            <a href="/comunicados/edit/{{this.id}}" class="text-warning"><i
                                    class="fa-solid fa-pencil"></i>Edit</a>
                            <a href="/comunicados/del/{{this.id}}" class="text-warning"><i
                                    class="fa-solid fa-trash"></i>Del</a>
                        </td> 
                    </tr>
                    {{/each}}
                </table>
                {{/if}}
            </div>
        </div>
    </div>
              <div class="text-center m-2">
                        <a href="/comunicados/add/{{partida.id}}" class="btn btn-warning"> AÑADIR COMUNICADO</a>
                    </div>
</div>